import DisplayCore
import Foundation

/// HAL I/O (`HALDeviceEnumerator`, `HALVolumeControl`). Matcher is `DisplayCore.AudioMatching`.
public enum AudioKitModule {
    public static let version = "0.1.0"
}
